<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF000000">

    <!-- Contenedor de la cámara con barras laterales PANTALLA COMPLETA -->
    <FrameLayout
        android:id="@+id/cameraContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Barra negra izquierda con botones - AUMENTADA A 250dp -->
        <FrameLayout
            android:id="@+id/leftBlackBar"
            android:layout_width="250dp"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="#FF000000">

            <!-- Botones en la barra izquierda -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:orientation="vertical"
                android:gravity="center">

                <!-- Botón para volver -->
                <Button
                    android:id="@+id/btnBack"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_marginBottom="24dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:text="←"
                    android:textColor="#FFFFFFFF"
                    android:textSize="20sp" />

                <!-- Botón para iniciar cámara -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnStartCamera"
                    android:layout_width="60dp"
                    android:layout_height="48dp"
                    android:layout_marginBottom="24dp"
                    android:text="Iniciar"
                    android:textAllCaps="false"
                    android:textSize="8sp"
                    app:cornerRadius="24dp"
                    android:textColor="#FFFFFFFF"
                    app:backgroundTint="@color/red_primary" />

                <!-- Botón para cambiar cámara - AHORA CON ICONO -->
                <ImageButton
                    android:id="@+id/btnSwitchCamera"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_marginBottom="24dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_switch_camera"
                    app:tint="#FFFFFFFF"
                    android:contentDescription="Cambiar cámara" />

                <!-- Botón para ver archivos -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnViewFiles"
                    android:layout_width="60dp"
                    android:layout_height="48dp"
                    android:text="📁 Ver"
                    android:textAllCaps="false"
                    android:textSize="11sp"
                    app:cornerRadius="24dp"
                    android:textColor="#FFFFFFFF"
                    app:backgroundTint="@color/red_primary" />

            </LinearLayout>

        </FrameLayout>

        <!-- Barra negra derecha - AUMENTADA A 250dp -->
        <View
            android:id="@+id/rightBlackBar"
            android:layout_width="250dp"
            android:layout_height="match_parent"
            android:background="#FF000000"
            android:layout_gravity="end" />

        <!-- Vista previa de la cámara (centrada entre las barras) - MARGENES ACTUALIZADOS -->
        <androidx.camera.view.PreviewView
            android:id="@+id/previewView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="250dp"
            android:layout_marginEnd="250dp" >

            <!-- Overlay de orientación horizontal DENTRO del PreviewView -->
            <LinearLayout
                android:id="@+id/orientationOverlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#CC000000"
                android:gravity="center"
                android:orientation="vertical"
                android:visibility="gone">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="16dp"
                    android:text="🔄"
                    android:textSize="48sp"
                    android:textColor="#FFFFFFFF" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="8dp"
                    android:gravity="center"
                    android:text="Gira tu teléfono a horizontal"
                    android:textColor="#FFFFFFFF"
                    android:textSize="18sp"
                    android:textStyle="bold" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:alpha="0.8"
                    android:gravity="center"
                    android:text="Para una mejor experiencia de grabación"
                    android:textColor="#FFFFFFFF"
                    android:textSize="14sp" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnContinuePortrait"
                    style="@style/Widget.Material3.Button.OutlinedButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="24dp"
                    android:text="Continuar en vertical"
                    android:textAllCaps="false"
                    android:textColor="#FFFFFFFF"
                    app:strokeColor="#FFFFFFFF" />

            </LinearLayout>
        </androidx.camera.view.PreviewView>

    </FrameLayout>

    <!-- Barra superior con indicadores - FONDO TRANSPARENTE -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:baselineAligned="false"
        android:background="@android:color/transparent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Indicador de grabación -->
        <LinearLayout
            android:id="@+id/recordingIndicator"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:visibility="invisible">

            <View
                android:id="@+id/recordingDot"
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:background="@drawable/recording_dot" />

            <TextView
                android:id="@+id/recordingText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Grabando"
                android:textColor="#FFFFFFFF"
                android:textSize="14sp"
                android:layout_marginStart="8dp" />
        </LinearLayout>

        <!-- Indicador de detección de manos -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <View
                android:id="@+id/handIndicator"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:background="@drawable/indicator_circle" />

            <TextView
                android:id="@+id/handStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Sin manos"
                android:textColor="#FFFFFFFF"
                android:textSize="14sp"
                android:layout_marginStart="8dp" />
        </LinearLayout>
    </LinearLayout>

    <!-- Indicador de modo de procesamiento -->
    <LinearLayout
        android:id="@+id/modeIndicator"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="12dp"
        android:background="#80000000"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="16dp"
        android:clickable="true"
        android:focusable="true">

        <TextView
            android:id="@+id/modeText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📝 Landmarks"
            android:textColor="#FFFFFFFF"
            android:textSize="12sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- Mensaje de orientación en la parte superior (alternativo) -->
    <LinearLayout
        android:id="@+id/orientationHint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="8dp"
        android:background="#FF4CAF50"
        android:visibility="gone"
        android:baselineAligned="false"
        app:layout_constraintTop_toBottomOf="@id/modeIndicator">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔄"
            android:textSize="16sp"
            android:layout_marginEnd="8dp"
            android:textColor="#FFFFFFFF" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Orientación horizontal recomendada"
            android:textColor="#FFFFFFFF"
            android:textSize="12sp" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>